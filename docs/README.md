Supporting material for the top-level README.

- `before-after.png` – crops of the three slides below, stacked, used at the top of the README.
- `copy-paste-probs.pptx` – a deck built in PowerPoint for Mac with the same figure pasted
  three times: plain ⌘V (PDF flavour, stored by PowerPoint as EMF), ⌃⌘V Paste Special as
  TIFF, and after running hiresclip (600 dpi PNG).
- `copy-paste-probs.pdf` – the same three slides exported from PowerPoint for Windows.
